CC = nvcc
FLAGS = -I /usr/local/cudpp_src_2.0/include/
cudpplib = /usr/local/cudpp/lib/libcudpp.so /usr/local/cudpp/lib/libcudpp_hash.so

objects = main.o $(cudpplib)

cudpptest : $(objects)
	$(CC) -o cudpptest $(objects) $(FLAGS)

main.o : main.cu
	$(CC) -c main.cu $(FLAGS)
